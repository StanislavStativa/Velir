import { Default as BackgroundThumbnail } from './BackgroundThumbnail.dev';

import { defaultMockData } from './backgound-tumbnail.mock';

export default {
  title: 'Components/Background Thumbnail',
  component: BackgroundThumbnail,
  tags: ['autodocs'],
};

export const Default = {
  args: defaultMockData,
};
